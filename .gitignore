node_modules/
release/
dist/
.DS_Store
*.log

# Local Supabase connection (copy from cloud-config.example.json).
# The anon key is safe to ship, but keep local overrides out of git by default.
src/cloud/cloud-config.json
